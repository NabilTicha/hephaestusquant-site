{
  "name": "hephaestusquant-site",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --d1 FORECAST_DB",
    "deploy": "wrangler pages deploy .",
    "db:init": "wrangler d1 execute FORECAST_DB --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute FORECAST_DB --remote --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
